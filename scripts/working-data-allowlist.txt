# working_data Allowlist
#
# Phase 6A: working_data freeze in effect
# Only pre-existing legacy writers are permitted below.
# NO NEW ENTRIES may be added - use normalized tables + commands.
#
# New persistence targets:
# - Pricing: pricing_scenarios + pricing_lines tables
# - Requirements: requirements table with intelligence_version_id
# - Charge codes: proposal_charge_codes table
# - Team data: proposal_metadata JSONB column
#
# Legacy writers (pending migration):
app/api/proposals/[id]/route.ts
