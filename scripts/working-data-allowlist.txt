# working_data Allowlist
#
# Phase 6A: working_data has ZERO writers
# This file is EMPTY - CI enforces no writes to working_data
#
# All persistence now uses normalized tables + commands:
# - Pricing: pricing_scenarios + pricing_lines tables
# - Requirements: requirements table with intelligence_version_id
# - Charge codes: proposal_charge_codes table
# - Team data: proposal_metadata JSONB column (read-only from working_data)
#
# working_data is READ-ONLY legacy data retained for audit/fallback
